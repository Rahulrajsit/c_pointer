#ifndef DEMO_H
#define DEMO_H
#endif
#include <stdio.h>

prcess_string(const char *input) {
    // This function processes the input string and prints it
    // In a real application, you might do more complex processing here
    printf("Processing string: %s\n", input);
    printf("String length: %zu\n", strlen(input));
}
